import assert from 'node:assert/strict';
import { mkdirSync, writeFileSync } from 'node:fs';
import { canonicalInput } from './passive-hard-case-outcome-value-scorecard-fixtures.mjs';
import { scorePassiveHardCaseOutcomeValueScorecard, validatePassiveHardCaseOutcomeValueScorecardArtifact } from '../src/passive-hard-case-outcome-value-scorecard.mjs';
const out = scorePassiveHardCaseOutcomeValueScorecard(canonicalInput());
assert.deepEqual(validatePassiveHardCaseOutcomeValueScorecardArtifact(out), []);
assert.equal(out.rawSourceCache, 'excluded');
assert.equal(out.rawPersisted, false);
assert.equal(out.humanReadableReportOnly, true);
assert.equal(out.nonAuthoritative, true);
assert.equal(out.notRuntimeInstruction, true);
assert.equal(out.noRuntimeAuthority, true);
assert.equal(out.noMemoryWrites, true);
assert.equal(out.noRuntimeIntegration, true);
assert.equal(out.policyDecision, null);
assert.ok(out.reportMarkdown.includes('not policy decisions'));
mkdirSync('evidence',{recursive:true}); writeFileSync('evidence/passive-hard-case-outcome-value-scorecard-output-boundary-v0.34.4.out.json', JSON.stringify({status:'pass',policyDecision:out.policyDecision,boundaryViolationCount:out.metrics.boundaryViolationCount},null,2)+'\n');
