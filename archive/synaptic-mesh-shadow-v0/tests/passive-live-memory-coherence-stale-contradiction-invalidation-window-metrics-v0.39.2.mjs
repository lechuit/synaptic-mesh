import assert from 'node:assert/strict';
import { mkdirSync, writeFileSync } from 'node:fs';
import { canonicalInput } from './passive-live-memory-coherence-stale-contradiction-invalidation-window-fixtures.mjs';
import { scorePassiveLiveMemoryCoherenceInvalidationWindow } from '../src/passive-live-memory-coherence-stale-contradiction-invalidation-window.mjs';
const out = scorePassiveLiveMemoryCoherenceInvalidationWindow(canonicalInput());
const m = out.metrics;
assert.equal(m.invalidationWindowCount, 1);
assert.equal(m.candidateSignalCount, 5);
assert.equal(m.validCarryForwardCount, 3);
assert.equal(m.staleSignalCount, 1);
assert.equal(m.contradictionSignalCount, 1);
assert.equal(m.staleInvalidatedCount, 1);
assert.equal(m.contradictionCautionCount, 1);
assert.equal(m.includedForHumanHandoffCount, 3);
assert.equal(m.invalidatedOrCautionedSignalCount, 2);
assert.equal(m.sourceBoundInvalidationRatio, 1);
assert.equal(m.staleInvalidationRatio, 1);
assert.equal(m.contradictionCautionRatio, 1);
assert.equal(m.validCarryForwardRatio, 1);
assert.equal(m.redactedBeforePersistRatio, 1);
assert.equal(m.rawPersistedFalseRatio, 1);
assert.equal(m.humanReviewOnlyRatio, 1);
assert.equal(m.noPromotionWithoutHumanRatio, 1);
assert.equal(m.agentConsumedOutputFalseRatio, 1);
assert.equal(m.boundaryViolationCount, 0);
assert.equal(m.policyDecision, null);
mkdirSync('evidence',{recursive:true});
writeFileSync('evidence/passive-live-memory-coherence-stale-contradiction-invalidation-window-metrics-v0.39.2.out.json', JSON.stringify(m,null,2)+'\n');
