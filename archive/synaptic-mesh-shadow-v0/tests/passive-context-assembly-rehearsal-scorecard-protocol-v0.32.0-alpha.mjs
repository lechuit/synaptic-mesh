import assert from 'node:assert/strict';
import { writeFileSync, mkdirSync } from 'node:fs';
import { passiveContextAssemblyRehearsalScorecardProtocol } from '../src/passive-context-assembly-rehearsal-scorecard.mjs';
const p=passiveContextAssemblyRehearsalScorecardProtocol();
assert.equal(p.releaseLayer,'v0.32.0-alpha');
assert.equal(p.barrierCrossed,'passive_context_assembly_rehearsal_for_ai_continuity');
assert.equal(p.acceptsOnlyPinnedCompletedConflictScorecard,true);
assert.equal(p.humanReadableReportOnly,true); assert.equal(p.nonAuthoritative,true); assert.equal(p.policyDecision,null);
assert.equal(p.noMemoryWrites,true); assert.equal(p.noRuntimeIntegration,true); assert.equal(p.agentConsumedOutput,false);
mkdirSync('evidence',{recursive:true}); writeFileSync('evidence/passive-context-assembly-rehearsal-scorecard-protocol-v0.32.0-alpha.out.json', JSON.stringify(p,null,2)+'\n');
