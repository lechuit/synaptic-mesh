import assert from 'node:assert/strict';
import { mkdir, writeFile } from 'node:fs/promises';
import { canonicalInput } from './passive-source-authority-conflict-scorecard-fixtures.mjs';
import { scorePassiveSourceAuthorityConflictScorecard } from '../src/passive-source-authority-conflict-scorecard.mjs';
const out = scorePassiveSourceAuthorityConflictScorecard(canonicalInput());
assert.equal(out.metrics.conflictCaseCount, 4);
assert.equal(out.metrics.sourceBoundConflictRatio, 1);
assert.equal(out.metrics.authorityConflictSurfacedRatio, 1);
assert.equal(out.metrics.newerSourcePreferredRatio, 1);
assert.equal(out.metrics.contradictionConflictSurfacedRatio, 1);
assert.equal(out.metrics.staleInvalidationCautionRatio, 1);
assert.equal(out.metrics.humanReviewOnlyRatio, 1);
assert.equal(out.metrics.noPromotionWithoutHumanRatio, 1);
assert.equal(out.metrics.boundaryViolationCount, 0);
assert.equal(out.metrics.policyDecision, null);
assert.equal(out.recommendation, 'ADVANCE_OBSERVATION_ONLY');
await mkdir('evidence', { recursive: true });
await writeFile('evidence/passive-source-authority-conflict-scorecard-metrics-v0.31.2.out.json', `${JSON.stringify(out.metrics, null, 2)}\n`);
