# Status v0.18.2 — preflight aborts

Abort gates cover network/resource fetch, writes, tools, daemon/watch, batch, excess N, raw persistence/output, policy decisions, agent-consumed output, private token leakage, approval/block/allow/authorization/enforcement, and external effects.
