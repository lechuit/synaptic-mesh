# Bounded Explicit Multisource Shadow-Read Redaction/Retention v0.20.3

The adapter output is transformed before persistence. Raw source paths and raw records are not persisted. Evidence contains hashes, counts, source status, redacted previews, and a human-readable report. Decision verbs and private patterns are checked after redaction; semantic decision tokens must not persist.
