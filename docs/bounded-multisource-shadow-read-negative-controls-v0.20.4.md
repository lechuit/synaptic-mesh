# Bounded Explicit Multisource Shadow-Read Negative Controls v0.20.4

Negative controls cover single-source attempts, source-count excess, per-source and total record excess, unsupported/disallowed capability flags, network/resource fetch, daemon/watch mode, execution, memory/config writes, raw output/persistence, agent-consumed output, policy/approval/enforcement language, globs/recursive discovery, implicit manifests/batches, fixture positives, outside-repo paths, and symlinks. Expected result: all reject, `unexpectedPermits: 0`.
