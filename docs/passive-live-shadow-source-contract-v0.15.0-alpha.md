# v0.15.0-alpha passive live shadow source contract

Scope: explicit local file/sample streams only. The contract is disabled by default, human-started/manual, with no network fetch, no SDK/framework adapter, no daemon/watcher, and no actions. Any live-like input is represented only as a local sample/pipe fixture and is redaction-gated before persistence. No production/live deployment.
