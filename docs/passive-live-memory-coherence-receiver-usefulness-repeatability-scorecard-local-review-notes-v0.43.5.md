# Local Review Notes — v0.43.5 Receiver Usefulness Repeatability Scorecard

Two independent local reviews were completed before PR.

## Review A

Initial result: BLOCKED on stale `MANIFEST.files.json` hashes for regenerated historical evidence. Targeted v0.43 tests, pinned v0.42.5 artifact validation, generated v0.43.5 evidence validation, exact report generation, and boundary flags otherwise passed.

Final result after fixes: APPROVED.

Evidence checked:
- `npm run verify:manifest` passed with 1638 files.
- `npm run release:check -- --target v0.43.5` passed.
- Reviewer-package test stdout no longer emits `policyDecision`.
- Bin stdout no longer emits `policyDecision`.
- Reviewer artifact keeps `policyDecision` only as a top-level `null` compatibility sentinel; absent from protocol, metrics, repeatability runs, and judgements.
- Source artifact and embedded report hashes match the pinned v0.42.5 usefulness rehearsal artifact.
- Metrics match the expected three runs, fifteen judgements, five stable candidates, zero unstable candidates, repeatability ratios 1, and zero boundary violations.
- Negative controls cover 16 rejected cases, including two pre-read path pinning controls.
- Exact report equality holds.
- Local/passive/read-only/no runtime authority/no memory writes/no agent consumption boundaries remain intact.

## Review B

Initial result: BLOCKED on stale manifest hashes and repeated machine-readable `policyDecision: null` fields in protocol/metrics/per-run/per-judgement/stdout.

Final result after fixes: APPROVED.

Evidence checked:
- `npm run verify:manifest` passed with 1638 files.
- `npm run release:check -- --target v0.43.5` passed with release target and manifest target `v0.43.5`.
- v0.43 bin stdout and reviewer-package test stdout contain zero `policyDecision` occurrences.
- Reviewer JSON has only one `policyDecision` field path: top-level `policyDecision: null`; none under protocol, metrics, runs, or judgements.
- Pre-read path pinning remains `preReadPathPinned: true`.
- Source artifact SHA and source report SHA match the pinned upstream evidence.
- Metrics verified: 3 runs, 15 judgements, 5 stable, 0 unstable, boundary violations 0.
- Boundary flags intact: local/passive/read-only, no runtime authority/integration, no memory writes, no agent-consumed output, raw source excluded, raw persisted false.

## Non-blocking notes

- The top-level `policyDecision: null` field remains only for compatibility with prior release-check/report conventions. It is not repeated in protocol, metrics, per-run, per-judgement, or CLI/test stdout.
- Historical v0.18/v0.19/v0.20 evidence files were regenerated by existing gates against current README/RELEASE_NOTES text and reconciled in `MANIFEST.files.json`; this is expected for current release-check behavior.
