# Status v0.14.3

Status: sandbox failure catalog and negative controls ready for dangerous live-like labels.

Covered labels: tool request, approval-ish text, stale source, missing redaction, and framework-looking route. Expected handling: fail closed/degrade/record-only. unexpectedPermits: 0.

Boundary remains local, offline/frozen/already-redacted, no tools, no live traffic, no network, no runtime authority, no agent-consumed output, no machine-readable policy decision, and no approval/block/allow/authorization/enforcement.
