# Negative Controls v0.21.4

Negative controls reject pass-to-human-review when records are missing, bounds are invalid, source failures exceed the configured threshold, redaction leaks are present, raw persisted/output fields are true, `policyDecision` is non-null, agent-consumed output is true, or forbidden capabilities appear.
