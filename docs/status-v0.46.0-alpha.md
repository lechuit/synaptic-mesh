# Synaptic Mesh v0.46.0-alpha

Status slice for passive live memory/coherence receiver live-context handoff utility. Boundary: local/manual/passive/read-only, stable v0.45 signals only, human-readable report only, non-authoritative, no memory writes, no runtime integration, no agent-consumed output, and `policyDecision: null` only as a top-level compatibility sentinel.
