# Passive Live Memory/Coherence Invalidation Repeatability Scorecard — Runs v0.40.1

Run evidence covers three deterministic passive judgement variants over the same five v0.39.5 invalidation candidates:

- `baseline_order`
- `paraphrased_invalidation_rationale`
- `reverse_order`

Each run emits five human-review-only invalidation judgements. All runs keep `policyDecision: null` and `agentConsumedOutput: false`.
