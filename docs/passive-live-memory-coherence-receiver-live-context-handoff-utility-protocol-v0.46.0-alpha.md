# Passive Live Memory Coherence Receiver Live Context Handoff Utility Protocol v0.46.0-alpha

Defines the bounded handoff-utility protocol over the pinned v0.45.5 live-observation repeatability artifact. It is disabled by default, operator-run one-shot only, local/passive/read-only, explicit artifact only, pre-read path pinned, stable-signals-only, redacted before persist, human-readable only, non-authoritative, no memory writes, no runtime integration, no agent-consumed output, and `policyDecision: null` only as a top-level compatibility sentinel in the generated artifact.
