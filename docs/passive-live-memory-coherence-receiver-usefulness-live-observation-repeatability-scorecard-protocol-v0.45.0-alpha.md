# Passive Live Memory Coherence Receiver Usefulness Live Observation Repeatability Scorecard Protocol v0.45.0-alpha

Defines the bounded repeatability-only protocol over the pinned v0.44.5 live observation artifact. It is disabled by default, operator-run one-shot only, local/passive/read-only, explicit artifact only, pre-read path pinned, redacted before persist, human-readable only, non-authoritative, no memory writes, no runtime integration, no agent-consumed output, and `policyDecision: null` only as a top-level compatibility sentinel in the generated artifact.
