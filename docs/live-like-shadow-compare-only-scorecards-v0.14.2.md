# Live-like Shadow/Sandbox Compare-only Scorecards v0.14.2

This layer adds compare-only scorecards for baseline vs Synaptic Mesh local comparison.

Scope:

- compare-only
- advisory/comparison evidence only
- not policy decisions
- not agent-consumed output
- not runtime authority
- no approval/block/allow/authorization/enforcement
- no network
- no live traffic
- no tools
- no daemon or watcher
- no framework adapter or SDK import

Scorecards are local evidence artifacts. They do not authorize, block, allow, enforce, approve, or instruct an agent.

Evidence target: `release:check -- --target v0.14.2`.
