# Status v0.16.6

Tiny operator-run passive pilot input boundary hardening. Input is restricted to one package `fixtures/` `.txt` sample file. External paths, metadata/docs/evidence paths, directories, non-text fixtures, symlink input files, and symlink parent escapes are rejected before read.

Still not live runtime: no autonomous mode, watcher/daemon, network, tools, memory/config writes, agent consumption, policy decisions, authorization, enforcement, or external effects.
