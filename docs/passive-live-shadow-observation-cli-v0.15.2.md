# v0.15.2 passive observation CLI/harness

Manual invocation only, local input path only, writes local evidence or stdout. No watcher/daemon, no network, no tool execution, no runtime authority. Reject unsafe flags --watch --daemon --network --execute --allow --block --enforce --approve before doing work.
