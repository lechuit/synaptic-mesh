# Status v0.45.4

Output-boundary layer: validates exact report generation, top-level-only `policyDecision: null`, no policy decision in protocol/metrics/runs/items/stdout, no memory promotion, no raw persistence, no agent-consumed output, and no runtime authority.
