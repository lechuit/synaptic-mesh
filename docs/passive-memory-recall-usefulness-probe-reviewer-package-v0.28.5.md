# Passive memory recall usefulness probe Reviewer package

Release layer: v0.28.5.

This layer probes whether explicit redacted passive observation evidence can help recover continuity-relevant memory cards: decisions, project rules, contradictions, and stale negative context.

Boundary: local/manual/operator-run, read-only, explicit repo-local JSON artifacts only, redacted-before-persist, human-readable report only, non-authoritative, recommendationIsAuthority: false, policyDecision: null, no runtime integration, no memory/config writes, no tool execution, no network/resource fetch, no external effects.
