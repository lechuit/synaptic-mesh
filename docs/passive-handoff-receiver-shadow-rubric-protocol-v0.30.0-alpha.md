# Passive Handoff Receiver Shadow Rubric v0.30.0-alpha

Defines a passive receiver-side rubric over the pinned v0.29.5 handoff artifact. It is local/manual/read-only/human-readable and non-authoritative with policyDecision null.

Boundary: no enforcement, authorization, approval/block/allow, runtime integration, memory/config writes, network/resource fetch, external effects, or agent-consumed policy decisions. `policyDecision: null`.
