# Status v0.45.0-alpha

Passive live memory/coherence receiver usefulness live observation repeatability protocol: disabled-by-default, operator-run, local/passive/read-only, pinned v0.44.5 live observation only, human-readable only, no memory writes, no runtime integration, no agent-consumed output, and `policyDecision: null` only as a top-level compatibility sentinel.
