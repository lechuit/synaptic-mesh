# Status v0.14.0-alpha

Status: live-like shadow/sandbox observation envelope schema ready for local, manual, offline/frozen/already-redacted fixtures only.

Boundary: no raw private content, no live traffic, no network, no watcher/daemon, no SDK import, no framework adapter, no tools, not runtime authority, no agent-consumed output, no machine-readable policy decision, and no approval/block/allow/authorization/enforcement.
