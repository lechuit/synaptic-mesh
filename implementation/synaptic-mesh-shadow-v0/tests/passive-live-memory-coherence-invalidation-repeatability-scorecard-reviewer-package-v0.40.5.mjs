import assert from 'node:assert/strict';
import { mkdirSync, writeFileSync } from 'node:fs';
import { canonicalInput } from './passive-live-memory-coherence-invalidation-repeatability-scorecard-fixtures.mjs';
import { scorePassiveLiveMemoryCoherenceInvalidationRepeatabilityScorecard, validatePassiveLiveMemoryCoherenceInvalidationRepeatabilityScorecardArtifact, passiveLiveMemoryCoherenceInvalidationRepeatabilityScorecardReport } from '../src/passive-live-memory-coherence-invalidation-repeatability-scorecard.mjs';
const out = scorePassiveLiveMemoryCoherenceInvalidationRepeatabilityScorecard(canonicalInput());
out.reportMarkdown = passiveLiveMemoryCoherenceInvalidationRepeatabilityScorecardReport(out);
assert.equal(out.repeatabilityStatus, 'PASSIVE_LIVE_MEMORY_COHERENCE_INVALIDATION_REPEATABILITY_SCORECARD_COMPLETE');
assert.equal(out.recommendation, 'ADVANCE_OBSERVATION_ONLY');
assert.equal(out.policyDecision, null);
assert.deepEqual(validatePassiveLiveMemoryCoherenceInvalidationRepeatabilityScorecardArtifact(out), []);
mkdirSync('evidence',{recursive:true});
writeFileSync('evidence/passive-live-memory-coherence-invalidation-repeatability-scorecard-reviewer-package-v0.40.5.out.json', JSON.stringify(out,null,2)+'\n');
writeFileSync('evidence/passive-live-memory-coherence-invalidation-repeatability-scorecard-report-v0.40.5.out.md', out.reportMarkdown+'\n');
