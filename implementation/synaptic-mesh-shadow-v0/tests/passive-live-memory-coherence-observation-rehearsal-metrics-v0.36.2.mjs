import assert from 'node:assert/strict';
import { mkdirSync, writeFileSync } from 'node:fs';
import { canonicalInput } from './passive-live-memory-coherence-observation-rehearsal-fixtures.mjs';
import { scorePassiveLiveMemoryCoherenceObservationRehearsal } from '../src/passive-live-memory-coherence-observation-rehearsal.mjs';
const out = scorePassiveLiveMemoryCoherenceObservationRehearsal(canonicalInput());
assert.equal(out.metrics.explicitRepoLocalSourceCount, 4);
assert.equal(out.metrics.observationItemCount, 4);
assert.equal(out.metrics.sourceBoundObservationCount, 4);
assert.equal(out.metrics.includeForHumanContextCount, 3);
assert.equal(out.metrics.hardeningCautionCount, 1);
assert.equal(out.metrics.redactedBeforePersistRatio, 1);
assert.equal(out.metrics.rawPersistedFalseRatio, 1);
assert.equal(out.metrics.humanReviewOnlyRatio, 1);
assert.equal(out.metrics.noPromotionWithoutHumanRatio, 1);
assert.equal(out.metrics.agentConsumedOutputFalseRatio, 1);
assert.equal(out.metrics.sourceBoundObservationRatio, 1);
assert.equal(out.metrics.boundaryViolationCount, 0);
assert.equal(out.metrics.policyDecision, null);
assert.equal(out.recommendation, 'ADVANCE_OBSERVATION_ONLY');
mkdirSync('evidence',{recursive:true});
writeFileSync('evidence/passive-live-memory-coherence-observation-rehearsal-metrics-v0.36.2.out.json', JSON.stringify({ metrics: out.metrics, recommendation: out.recommendation }, null, 2)+'\n');
