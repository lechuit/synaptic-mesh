import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { scorePassiveLiveMemoryCoherenceReceiverRuntimeInvocationShimRehearsal, validatePassiveLiveMemoryCoherenceReceiverRuntimeInvocationShimRehearsalArtifact } from '../src/passive-live-memory-coherence-receiver-runtime-invocation-shim-rehearsal.mjs';
import { receiverRuntimeInvocationShimRehearsalInputV051 } from './passive-live-memory-coherence-receiver-runtime-invocation-shim-rehearsal-fixtures.mjs';
const artifact = scorePassiveLiveMemoryCoherenceReceiverRuntimeInvocationShimRehearsal(receiverRuntimeInvocationShimRehearsalInputV051());
assert.deepEqual(validatePassiveLiveMemoryCoherenceReceiverRuntimeInvocationShimRehearsalArtifact(artifact), []);
assert.equal(artifact.recommendation, 'ADVANCE_TO_BOUNDED_NO_EFFECT_RECEIVER_RUNTIME_SHIM_ADAPTER_FIXTURE_SUITE');
const report = artifact.reportMarkdown;
for (const phrase of ['Receiver Runtime Invocation Shim Rehearsal v0.51.5','PASSIVE_LIVE_MEMORY_COHERENCE_RECEIVER_RUNTIME_INVOCATION_SHIM_REHEARSAL_COMPLETE','localShimInvocationCount=4','shimOutputCount=4','contextHandoffResultCount=1','effectsBlockedCount=10','boundaryViolationCount=0','operatorApprovalScope: local_receiver_runtime_invocation_shim_rehearsal_only','deterministic local receiver runtime invocation shim']) assert(report.includes(phrase), phrase);
assert.equal(readFileSync('evidence/passive-live-memory-coherence-receiver-runtime-invocation-shim-rehearsal-report-v0.51.5.out.md', 'utf8'), `${artifact.reportMarkdown}\n`);
assert(readFileSync('../../README.md','utf8').includes('v0.51.5'));
assert(readFileSync('../../RELEASE_NOTES.md','utf8').includes('v0.51.5'));
console.log(JSON.stringify({ rehearsalStatus: artifact.rehearsalStatus, localShimInvocationCount: artifact.metrics.localShimInvocationCount }));
