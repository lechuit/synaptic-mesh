import assert from 'node:assert/strict';
import { mkdir, writeFile } from 'node:fs/promises';
import { canonicalInput } from './passive-handoff-receiver-shadow-rubric-fixtures.mjs';
import { scorePassiveHandoffReceiverRubric } from '../src/passive-handoff-receiver-shadow-rubric.mjs';
const out = scorePassiveHandoffReceiverRubric(canonicalInput());
assert.equal(out.metrics.receiverItemCount, 4);
assert.equal(out.metrics.includeForHumanContextCount, 2);
assert.equal(out.metrics.contradictionReviewCount, 1);
assert.equal(out.metrics.staleCautionReviewCount, 1);
assert.equal(out.metrics.excludedUpstreamNoiseCount, 1);
assert.equal(out.metrics.sourceBoundReceiverRatio, 1);
assert.equal(out.metrics.contradictionHandledRatio, 1);
assert.equal(out.metrics.staleHandledRatio, 1);
assert.equal(out.metrics.noPromotionWithoutHumanRatio, 1);
assert.equal(out.metrics.boundaryViolationCount, 0);
assert.equal(out.metrics.policyDecision, null);
assert.equal(out.recommendation, 'ADVANCE_OBSERVATION_ONLY');
await mkdir('evidence', { recursive: true });
await writeFile('evidence/passive-handoff-receiver-shadow-rubric-metrics-v0.30.2.out.json', `${JSON.stringify(out.metrics, null, 2)}\n`);
