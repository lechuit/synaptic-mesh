import assert from 'node:assert/strict';
import { receiverUsefulnessLiveObservationArtifactFixture } from './passive-live-memory-coherence-receiver-usefulness-live-observation-fixtures.mjs';

const artifact = await receiverUsefulnessLiveObservationArtifactFixture();
const m = artifact.metrics;
assert.equal(m.explicitRepoLocalSourceCount, 4);
assert.equal(m.sourceRepeatabilityRunCount, 3);
assert.equal(m.sourceCandidateCount, 5);
assert.equal(m.sourceStableCandidateCount, 5);
assert.equal(m.liveObservationItemCount, 5);
assert.equal(m.sourceBoundObservationCount, 5);
assert.equal(m.includedForHumanContextCount, 3);
assert.equal(m.staleSuppressedObservationCount, 1);
assert.equal(m.contradictionCautionObservationCount, 1);
assert.equal(m.receiverUsefulLiveContextItemCount, 4);
assert.equal(m.receiverNoisyLiveContextItemCount, 0);
assert.equal(m.rawSignalUsefulItemCount, 3);
assert.equal(m.rawSignalNoisyItemCount, 2);
assert.equal(m.scorecardUsefulItemCount, 3);
assert.equal(m.scorecardNoisyItemCount, 2);
assert.equal(m.receiverUsefulnessRatio, 1);
assert.equal(m.rawSignalUsefulnessRatio, 0.6);
assert.equal(m.scorecardUsefulnessRatio, 0.6);
assert.equal(m.sourceBoundObservationRatio, 1);
assert.equal(m.redactedBeforePersistRatio, 1);
assert.equal(m.rawPersistedFalseRatio, 1);
assert.equal(m.humanReviewOnlyRatio, 1);
assert.equal(m.noPromotionWithoutHumanRatio, 1);
assert.equal(m.agentConsumedOutputFalseRatio, 1);
assert.equal(m.preReadPathPinned, true);
assert.equal(m.boundaryViolationCount, 0);
assert.equal(artifact.recommendation, 'ADVANCE_OBSERVATION_ONLY');
console.log(JSON.stringify({ recommendation: artifact.recommendation, metrics: m }));
