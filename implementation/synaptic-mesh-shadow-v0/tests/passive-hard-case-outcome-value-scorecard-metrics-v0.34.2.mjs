import assert from 'node:assert/strict';
import { mkdirSync, writeFileSync } from 'node:fs';
import { canonicalInput } from './passive-hard-case-outcome-value-scorecard-fixtures.mjs';
import { scorePassiveHardCaseOutcomeValueScorecard } from '../src/passive-hard-case-outcome-value-scorecard.mjs';
const out = scorePassiveHardCaseOutcomeValueScorecard(canonicalInput());
assert.equal(out.metrics.outcomeCount, 5);
assert.equal(out.metrics.usefulOutcomeCount, 3);
assert.equal(out.metrics.noiseOutcomeCount, 1);
assert.equal(out.metrics.evidenceGapOutcomeCount, 1);
assert.equal(out.metrics.usefulOutcomeRatio, 0.6);
assert.equal(out.metrics.noiseOutcomeRatio, 0.2);
assert.equal(out.metrics.evidenceGapRatio, 0.2);
assert.equal(out.metrics.sourceBoundOutcomeRatio, 1);
assert.equal(out.metrics.humanReviewOnlyRatio, 1);
assert.equal(out.metrics.minimalContextRatio, 1);
assert.equal(out.metrics.noPromotionWithoutHumanRatio, 1);
assert.equal(out.metrics.boundaryViolationCount, 0);
assert.equal(out.metrics.policyDecision, null);
assert.equal(out.recommendation, 'ADVANCE_OBSERVATION_ONLY');
mkdirSync('evidence',{recursive:true}); writeFileSync('evidence/passive-hard-case-outcome-value-scorecard-metrics-v0.34.2.out.json', JSON.stringify(out.metrics,null,2)+'\n');
