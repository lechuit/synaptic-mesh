# Curated reproducibility fixtures

This directory contains the 15 priority fixture artifacts referenced by `implementation/synaptic-mesh-shadow-v0/fixtures/manifest.json`. They are included so `npm --prefix implementation/synaptic-mesh-shadow-v0 run review:local` can run inside the staged review bundle.

These fixtures are local research evidence, not production/canary/enforcement authorization.
