# Synaptic Mesh v0.14.0-alpha

This is the public review release `v0.14.0-alpha`. Current v0.14.0-alpha status is narrower than live runtime: live-like shadow/sandbox observation envelopes for offline/frozen/already-redacted local/manual fixtures only.

The v0.14.x ladder moves toward live-like shadow/sandbox readiness while preserving a hard no-effects boundary. This layer adds only the envelope schema and one already-redacted fixture shape.

Boundaries: no runtime integration, no raw private content, no live traffic, no real framework adapter, no MCP server/client, no LangGraph SDK, no A2A runtime, no GitHub bot/webhook, no network/resource fetch, no tool execution, no watcher/daemon, no memory/config writes, no agent-consumed output, no machine-readable policy decision, not runtime authority, and no approval/block/allow/authorization/enforcement.

Final evidence: envelope schema present; offline/frozen/already-redacted: true; manual-or-frozen only: true; unexpectedPermits: 0.
